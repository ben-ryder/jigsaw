import { Meta, ColorPalette, ColorItem } from "@storybook/addon-docs/blocks";

<Meta title="Tokens/Surfaces" />

# Surfaces
Surface colours are **semantic-level tokens** used to define the background colours of "surfaces"
such as the main background colour, panels, dialogs, popovers etc.

Components such as dialogs, cards and popover menus inherit colours from these surface tokens, but they
will define component-level tokens such as `dialog-bg` rather than using them directly.
This allows the design system to stay consistent, but offer an escape hatch for applications if a specific use case requires
a different colour.

## Surfaces
| Surface              | Description                                                                                  |
|----------------------|----------------------------------------------------------------------------------------------|
| `j-surface-base`     | The main background                                                                          |
| `j-surface-panel`    | Used for UI elements like sidebars, main navigations and cards                               |
| `j-surface-overlay`  | Used for UI elements which appear over other content such as dialogs, popovers, tooltips etc |
| `j-surface-backdrop` | Used as the shadow colour for overlay UI elements                                            |

### Preview
<br/>

<div class="theme-dark">
  <ColorPalette>
    <ColorItem
      title="Dark Theme"
      subtitle="Surfaces for dark theme"
      colors={{
        "surface-base-bg": "var(--j-navy-30)",
        "surface-panel-bg": "var(--j-navy-40);",
        "surface-overlay-bg": "var(--j-navy-40)",
        "surface-backdrop-bg": "var(--j-navy-20)",
      }}
    />
  </ColorPalette>
</div>

<div class="j-theme-light">
  <ColorPalette>
    <ColorItem
      title="Light Theme"
      subtitle="Surfaces for light theme"
      colors={{
        "surface-base-bg": "var(--j-white-60)",
        "surface-panel-bg": "var(--j-white-40)",
        "surface-overlay-bg": "var(--j-white-60)",
        "surface-backdrop-bg": "var(--j-black-60)",
      }}
    />
  </ColorPalette>
</div>

## Utility Classes
Utility classes are provided so surfaces can be defined without repeating CSS rules. These classes
will apply the background and text colours for the given surface layer.

- `j-surface-base`
- `j-surface-panel`
- `j-surface-overlay`
- `j-surface-backdrop`
