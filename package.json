{
  "name": "@ben-ryder/jigsaw-base",
  "description": "A design system and component libraries for use in my personal projects.",
  "version": "0.0.0",
  "license": "AGPL-3.0-only",
  "author": "ben-ryder",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ben-ryder/jigsaw.git"
  },
  "bugs": {
    "url": "https://github.com/ben-ryder/jigsaw/issues"
  },
  "scripts": {
    "prepare": "husky",
    "build": "(cd packages/react && npm run build) && (cd packages/core && npm run build)",
    "test": "lerna run test",
    "version": "lerna version --no-private",
    "publish": "lerna publish --no-private"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "husky": "^9.1.7",
    "lerna": "^8.2.3"
  }
}
